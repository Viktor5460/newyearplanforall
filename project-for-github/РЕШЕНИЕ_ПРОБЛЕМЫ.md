# 🔧 Решение проблемы: "Could not open requirements file"

## ❌ Проблема:
Render не может найти файл `requirements.txt`

## ✅ Решение:

### Вариант 1: Проверьте, что файл загружен в GitHub

1. Зайдите на GitHub в ваш репозиторий
2. Убедитесь, что файл `requirements.txt` виден в корне репозитория
3. Если его нет - добавьте его

### Вариант 2: Загрузите файл заново

Если файл отсутствует в GitHub, выполните в папке `project-for-github`:

```bash
git add requirements.txt
git commit -m "Add requirements.txt"
git push
```

### Вариант 3: Проверьте структуру репозитория

Убедитесь, что структура репозитория правильная:

```
ваш-репозиторий/
├── app.py
├── requirements.txt  ← должен быть здесь!
├── schedule_data.py
├── Procfile
├── templates/
└── static/
```

### Вариант 4: Если файл в подпапке

Если файлы были загружены в подпапку (например, `project-for-github/`), то нужно либо:
- Переместить все файлы на уровень выше (в корень репозитория)
- Или в Render указать "Root Directory": `project-for-github`

## 🎯 Рекомендуемое решение:

1. Убедитесь, что вы находитесь в папке `project-for-github`
2. Проверьте наличие файла: `dir requirements.txt`
3. Если файл есть, загрузите его в GitHub:
   ```bash
   git add requirements.txt
   git commit -m "Add requirements.txt"
   git push
   ```
4. В Render нажмите "Manual Deploy" → "Deploy latest commit"

## 📝 Проверка:

После добавления файла, в GitHub репозитории должен быть виден файл `requirements.txt` в корне, рядом с `app.py`.


